<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> | Sports Scheduler</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Inter', sans-serif; }
        @media print {
            .no-print { display: none; }
            body { background: white; }
            .shadow-xl { box-shadow: none; }
        }
    </style>
</head>
<body class="bg-blue-50 min-h-screen p-8">

    <div class="max-w-6xl mx-auto">
        
        <div class="flex justify-between items-center mb-8 no-print">
            <h1 class="text-3xl font-extrabold text-slate-800 tracking-tight">Session Reports</h1>
            <a href="/sports" class="flex items-center gap-2 text-slate-500 hover:text-blue-600 transition font-medium">
                <i class="fa-solid fa-arrow-left"></i> Back to Dashboard
            </a>
        </div>

        <div class="bg-white p-6 rounded-2xl shadow-sm border border-blue-100 mb-8 no-print">
            <form action="/reports" method="GET" class="flex flex-col md:flex-row gap-4 items-end">
                <div class="flex-1 w-full">
                    <label class="block text-sm font-bold text-slate-700 mb-2">From Date</label>
                    <input type="date" name="startDate" value="<%= startDate %>" required 
                        class="w-full px-4 py-2 rounded-lg border border-slate-300 focus:ring-2 focus:ring-blue-500 outline-none text-slate-700">
                </div>
                <div class="flex-1 w-full">
                    <label class="block text-sm font-bold text-slate-700 mb-2">To Date</label>
                    <input type="date" name="endDate" value="<%= endDate %>" required 
                        class="w-full px-4 py-2 rounded-lg border border-slate-300 focus:ring-2 focus:ring-blue-500 outline-none text-slate-700">
                </div>
                <button type="submit" class="w-full md:w-auto px-6 py-2.5 bg-blue-600 text-white font-bold rounded-lg hover:bg-blue-700 shadow-lg shadow-blue-500/30 transition">
                    Generate Report
                </button>
            </form>
        </div>

        <% if (sessions.length > 0) { %>
            
            <div class="grid grid-cols-1 md:grid-cols-4 gap-6 mb-8">
                <div class="bg-white p-6 rounded-xl border border-blue-100 shadow-sm flex items-center gap-4">
                    <div class="w-12 h-12 rounded-full bg-blue-50 text-blue-600 flex items-center justify-center text-xl">
                        <i class="fa-solid fa-list-check"></i>
                    </div>
                    <div>
                        <p class="text-xs text-slate-400 font-bold uppercase">Total Sessions</p>
                        <p class="text-2xl font-extrabold text-slate-800"><%= sessions.length %></p>
                    </div>
                </div>

                <% if (sportStats.length > 0) { %>
                    <div class="md:col-span-2 bg-gradient-to-r from-blue-600 to-indigo-600 p-6 rounded-xl shadow-lg text-white flex items-center justify-between">
                        <div>
                            <p class="text-xs text-blue-100 font-bold uppercase mb-1">Most Popular Sport</p>
                            <h3 class="text-2xl font-extrabold"><%= sportStats[0].Sport.title %></h3>
                            <p class="text-sm text-blue-100"><%= sportStats[0].dataValues.count %> sessions played</p>
                        </div>
                        <i class="fa-solid fa-trophy text-4xl text-yellow-300 opacity-80"></i>
                    </div>
                <% } %>
                
                <div class="bg-white p-6 rounded-xl border border-red-100 shadow-sm flex items-center gap-4">
                    <div class="w-12 h-12 rounded-full bg-red-50 text-red-600 flex items-center justify-center text-xl">
                        <i class="fa-solid fa-ban"></i>
                    </div>
                    <div>
                        <p class="text-xs text-slate-400 font-bold uppercase">Canceled</p>
                        <p class="text-2xl font-extrabold text-slate-800">
                            <%= sessions.filter(s => s.canceled).length %>
                        </p>
                    </div>
                </div>
            </div>

            <h3 class="text-lg font-bold text-slate-700 mb-4">Sport Breakdown</h3>
            <div class="flex flex-wrap gap-4 mb-8">
                <% sportStats.forEach(stat => { %>
                    <div class="bg-white px-5 py-3 rounded-lg border border-slate-200 shadow-sm flex items-center gap-3">
                        <span class="font-bold text-slate-700"><%= stat.Sport.title %></span>
                        <span class="bg-blue-100 text-blue-700 text-xs font-bold px-2 py-1 rounded-full">
                            <%= stat.dataValues.count %>
                        </span>
                    </div>
                <% }) %>
            </div>

            <div class="flex justify-between items-center mb-4 no-print">
                <h3 class="text-lg font-bold text-slate-700">Detailed Log</h3>
                <button onclick="window.print()" class="text-sm font-bold text-slate-500 hover:text-blue-600 flex items-center gap-2 transition">
                    <i class="fa-solid fa-print"></i> Print Report
                </button>
            </div>

            <div class="bg-white rounded-xl shadow-sm border border-slate-200 overflow-hidden">
                <table class="w-full text-left">
                    <thead class="bg-slate-50 text-slate-500 text-xs uppercase font-bold border-b border-slate-200">
                        <tr>
                            <th class="p-4">Date</th>
                            <th class="p-4">Sport</th>
                            <th class="p-4">Session Name</th>
                            <th class="p-4">Organizer</th>
                            <th class="p-4">Status</th>
                        </tr>
                    </thead>
                    <tbody class="divide-y divide-slate-100">
                        <% sessions.forEach(session => { %>
                            <tr class="hover:bg-blue-50/30 transition">
                                <td class="p-4 text-slate-500 text-sm font-mono">
                                    <%= session.date %>
                                </td>
                                <td class="p-4 font-bold text-slate-700"><%= session.Sport.title %></td>
                                <td class="p-4 text-slate-600"><%= session.sessionName %></td>
                                <td class="p-4 text-slate-600 text-sm"><%= session.User.firstName %></td>
                                <td class="p-4">
                                    <% if (session.canceled) { %>
                                        <span class="inline-block px-2 py-1 text-xs font-bold text-red-600 bg-red-100 rounded border border-red-200">
                                            Canceled
                                        </span>
                                    <% } else { %>
                                        <span class="inline-block px-2 py-1 text-xs font-bold text-emerald-600 bg-emerald-100 rounded border border-emerald-200">
                                            Active
                                        </span>
                                    <% } %>
                                </td>
                            </tr>
                        <% }) %>
                    </tbody>
                </table>
            </div>

        <% } else if (startDate) { %>
            <div class="text-center py-20 bg-white rounded-xl border-2 border-dashed border-slate-200">
                <i class="fa-solid fa-filter-circle-xmark text-4xl text-slate-300 mb-4"></i>
                <h3 class="text-lg font-bold text-slate-700">No Data Found</h3>
                <p class="text-slate-500">No sessions match the selected date range.</p>
            </div>
        <% } else { %>
            <div class="text-center py-20">
                <i class="fa-solid fa-chart-pie text-6xl text-blue-200 mb-4"></i>
                <h3 class="text-xl font-bold text-slate-700">Generate Your Report</h3>
                <p class="text-slate-500 mt-2">Select a Start Date and End Date to view statistics.</p>
            </div>
        <% } %>

    </div>

</body>
</html>