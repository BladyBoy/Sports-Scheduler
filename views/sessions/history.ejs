<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>History | <%= sport.title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100 min-h-screen p-8">

    <div class="max-w-5xl mx-auto">
        <div class="flex items-center justify-between mb-8">
            <h1 class="text-2xl font-bold text-slate-800">
                <span class="text-blue-600"><%= sport.title %></span> History
            </h1>
            <a href="/sports/<%= sport.id %>" class="text-gray-600 hover:text-blue-600 transition font-medium">
                <i class="fa-solid fa-arrow-left"></i> Back
            </a>
        </div>

        <% if (pastSessions.length > 0) { %>
            <div class="bg-white rounded-xl shadow-md overflow-hidden border border-gray-200">
                <table class="w-full text-left border-collapse">
                    <thead class="bg-slate-50 text-slate-500 uppercase text-xs font-bold">
                        <tr>
                            <th class="p-4 border-b">Session Name</th>
                            <th class="p-4 border-b">Date</th>
                            <th class="p-4 border-b">Venue</th>
                            <th class="p-4 border-b">Status</th>
                        </tr>
                    </thead>
                    <tbody class="divide-y divide-gray-100">
                        <% pastSessions.forEach(session => { %>
                            <tr class="hover:bg-blue-50 transition group">
                                <td class="p-4 font-semibold text-slate-700 group-hover:text-blue-700">
                                    <%= session.sessionName %>
                                </td>
                                <td class="p-4 text-gray-600">
                                    <%= session.date %> <span class="text-xs text-gray-400">at <%= session.time %></span>
                                </td>
                                <td class="p-4 text-gray-600"><%= session.venue %></td>
                                <td class="p-4">
                                    <% if (session.canceled) { %>
                                        <span class="bg-red-100 text-red-600 px-2 py-1 rounded text-xs font-bold">Canceled</span>
                                    <% } else { %>
                                        <span class="bg-gray-100 text-gray-600 px-2 py-1 rounded text-xs font-bold">Completed</span>
                                    <% } %>
                                </td>
                            </tr>
                        <% }) %>
                    </tbody>
                </table>
            </div>
        <% } else { %>
            <div class="text-center py-20 bg-white rounded-xl shadow-sm border border-dashed border-gray-300">
                <i class="fa-solid fa-clock-rotate-left text-4xl text-gray-300 mb-4"></i>
                <h3 class="text-lg font-medium text-gray-600">No History Found</h3>
                <p class="text-gray-400">Past sessions will appear here.</p>
            </div>
        <% } %>
    </div>

</body>
</html>