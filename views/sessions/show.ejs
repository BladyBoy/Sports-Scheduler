<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> | Sports Scheduler</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Inter', sans-serif; }
    </style>
</head>
<body class="bg-blue-50 min-h-screen p-8">

    <div class="max-w-4xl mx-auto">
        <a href="/sports/<%= session.Sport.id %>" class="inline-flex items-center text-slate-500 hover:text-blue-600 mb-6 transition font-medium">
            <i class="fa-solid fa-arrow-left mr-2"></i> Back to <%= session.Sport.title %>
        </a>

        <div class="bg-white rounded-2xl shadow-xl border border-blue-100 overflow-hidden mb-8">
            
            <div class="bg-gradient-to-r from-blue-600 to-indigo-700 p-8 text-white relative">
                <h1 class="text-3xl font-bold mb-2 tracking-tight"><%= session.sessionName %></h1>
                
                <div class="flex flex-wrap gap-6 text-blue-100 text-sm font-medium mt-4">
                    <span class="flex items-center gap-2 bg-white/10 px-3 py-1 rounded-full border border-white/10">
                        <i class="fa-regular fa-calendar"></i> <%= session.date %>
                    </span>
                    <span class="flex items-center gap-2 bg-white/10 px-3 py-1 rounded-full border border-white/10">
                        <i class="fa-regular fa-clock"></i> <%= session.time %>
                    </span>
                    <span class="flex items-center gap-2 bg-white/10 px-3 py-1 rounded-full border border-white/10">
                        <i class="fa-solid fa-map-pin"></i> <%= session.venue %>
                    </span>
                </div>

                <% if (isOrganizer) { %>
                    <span class="absolute top-6 right-6 bg-yellow-400 text-yellow-900 text-xs font-bold px-3 py-1 rounded-full shadow-lg border border-yellow-300">
                        <i class="fa-solid fa-crown mr-1"></i> Organizer
                    </span>
                <% } %>
            </div>
            
            <div class="p-6 flex flex-col md:flex-row justify-between items-center gap-4 bg-blue-50/50 border-b border-blue-100">
                <div class="text-center md:text-left">
                    <p class="text-sm text-slate-500">Host: <span class="font-bold text-slate-800"><%= session.User.firstName %></span></p>
                    <p class="text-sm text-slate-500">Slots Left: <span class="text-emerald-600 font-bold text-lg"><%= session.playersNeeded %></span></p>
                </div>
                
                <% if (session.canceled) { %>
                    <div class="w-full md:w-auto bg-red-50 border border-red-200 text-red-700 px-4 py-2 rounded-lg text-center shadow-sm">
                        <p class="font-bold text-sm flex items-center justify-center gap-2">
                            <i class="fa-solid fa-ban"></i> SESSION CANCELED
                        </p>
                        <p class="text-xs mt-1 text-red-600">Reason: "<%= session.message %>"</p>
                    </div>

                <% } else if (isOrganizer) { %>
                    <div class="flex gap-2">
                        <a href="/sessions/edit/<%= session.id %>" class="px-4 py-2 bg-white border border-slate-200 text-slate-600 text-sm font-bold rounded-lg hover:bg-slate-50 hover:border-slate-300 transition shadow-sm">
                            <i class="fa-solid fa-pen mr-1"></i> Edit
                        </a>
                        <a href="/sessions/cancel/<%= session.id %>" class="px-4 py-2 bg-red-50 text-red-600 text-sm font-bold rounded-lg hover:bg-red-100 border border-red-200 transition shadow-sm">
                            Cancel
                        </a>
                    </div>
                
                <% } else if (isJoined) { %>
                    <form action="/sessions/leave" method="POST">
                        <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                        <input type="hidden" name="sessionId" value="<%= session.id %>">
                        <button type="submit" class="px-6 py-2 bg-red-100 text-red-700 font-semibold rounded-lg hover:bg-red-200 transition border border-red-200 shadow-sm">
                            Leave Session
                        </button>
                    </form>

                <% } else if (session.playersNeeded > 0) { %>
                    <form action="/sessions/join" method="POST">
                        <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                        <input type="hidden" name="sessionId" value="<%= session.id %>">
                        <button type="submit" class="px-6 py-2 bg-emerald-600 text-white font-semibold rounded-lg hover:bg-emerald-700 shadow-md shadow-emerald-500/30 transition transform hover:-translate-y-0.5">
                            Join Team
                        </button>
                    </form>

                <% } else { %>
                    <button disabled class="px-6 py-2 bg-slate-200 text-slate-400 font-semibold rounded-lg cursor-not-allowed border border-slate-300">
                        Session Full
                    </button>
                <% } %>
                </div>
        </div>

        <h3 class="text-xl font-bold text-slate-700 mb-4 flex items-center gap-2">
            <span class="w-1 h-6 bg-blue-500 rounded-full"></span>
            Squad List
        </h3>
        
        <div class="bg-white rounded-xl shadow-sm border border-blue-100 overflow-hidden">
            <% if (session.PlayerSessions && session.PlayerSessions.length > 0) { %>
                <table class="w-full text-left">
                    <thead class="bg-slate-50 text-slate-500 text-xs uppercase font-semibold tracking-wider">
                        <tr>
                            <th class="p-4 border-b border-slate-100">Player Name</th>
                            <th class="p-4 border-b border-slate-100">Role</th>
                            <% if (isOrganizer) { %> <th class="p-4 border-b border-slate-100 text-right">Actions</th> <% } %>
                        </tr>
                    </thead>
                    <tbody class="divide-y divide-slate-50">
                        <% session.PlayerSessions.forEach(player => { %>
                            <tr class="hover:bg-blue-50/50 transition">
                                <td class="p-4 font-medium text-slate-700">
                                    <div class="flex items-center gap-3">
                                        <div class="w-8 h-8 rounded-full bg-slate-100 text-slate-500 flex items-center justify-center text-xs font-bold">
                                            <%= player.User.firstName.charAt(0) %>
                                        </div>
                                        <span><%= player.User.firstName %> <%= player.User.lastName %></span>
                                        <% if (player.userId === user.id) { %> 
                                            <span class="text-xs bg-blue-100 text-blue-600 px-2 py-0.5 rounded-full font-bold">You</span> 
                                        <% } %>
                                    </div>
                                </td>
                                <td class="p-4">
                                    <% if (player.userId === session.userId) { %>
                                        <span class="text-xs bg-purple-100 text-purple-700 px-2 py-1 rounded font-bold border border-purple-200">Host</span>
                                    <% } else { %>
                                        <span class="text-xs bg-emerald-100 text-emerald-700 px-2 py-1 rounded font-bold border border-emerald-200">Player</span>
                                    <% } %>
                                </td>
                                
                                <% if (isOrganizer && player.userId !== session.userId) { %>
                                    <td class="p-4 text-right">
                                        <form action="/sessions/remove-player/<%= player.id %>" method="POST" class="inline">
                                            <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                                            <button type="submit" class="text-red-400 hover:text-red-600 bg-red-50 hover:bg-red-100 p-2 rounded-lg transition" title="Kick Player" onclick="return confirm('Kick this player?')">
                                                <i class="fa-solid fa-user-xmark"></i>
                                            </button>
                                        </form>
                                    </td>
                                <% } else if (isOrganizer) { %>
                                    <td class="p-4"></td> 
                                <% } %>
                            </tr>
                        <% }) %>
                    </tbody>
                </table>
            <% } else { %>
                <div class="p-12 text-center text-slate-400 italic bg-slate-50/50">
                    <i class="fa-solid fa-users-slash text-2xl mb-2 opacity-50"></i>
                    <p>No players joined yet.</p>
                </div>
            <% } %>
        </div>

    </div>
</body>
</html>