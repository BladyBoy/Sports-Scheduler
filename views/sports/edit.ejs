<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Sport | Sports Scheduler</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Inter', sans-serif; }
    </style>
</head>
<body class="bg-blue-50 min-h-screen flex items-center justify-center py-10 font-sans relative">

    <div class="absolute top-0 left-0 w-full h-96 bg-gradient-to-b from-blue-300/30 via-blue-200/20 to-transparent pointer-events-none -z-10"></div>

    <div class="bg-white w-full max-w-md p-8 rounded-2xl shadow-xl border border-blue-100 relative z-10">
        
        <div class="mb-8 border-b border-slate-100 pb-4 text-center">
            <h1 class="text-2xl font-bold text-slate-800">Rename Sport</h1>
            <p class="text-slate-500 text-sm mt-1">Update the name for this category</p>
        </div>

        <form action="/sports/update/<%= sport.id %>" method="POST" class="space-y-6">
            <input type="hidden" name="_csrf" value="<%= csrfToken %>">
            
            <div>
                <label for="title" class="block text-sm font-semibold text-slate-700 mb-1">Sport Name</label>
                <div class="relative">
                    <i class="fa-solid fa-trophy absolute left-3 top-3.5 text-slate-400"></i>
                    <input type="text" name="title" id="title" value="<%= sport.title %>" required 
                        class="w-full pl-10 pr-4 py-3 rounded-lg border border-slate-300 focus:ring-2 focus:ring-blue-500 focus:border-blue-500 outline-none transition text-slate-700 placeholder-slate-400">
                </div>
            </div>

            <div class="flex gap-4 pt-2">
                <a href="/sports" class="w-1/2 py-3 text-center text-slate-600 bg-slate-100 rounded-lg hover:bg-slate-200 transition font-medium">Cancel</a>
                <button type="submit" class="w-1/2 py-3 text-white bg-blue-600 rounded-lg hover:bg-blue-700 shadow-lg shadow-blue-500/30 transition font-medium transform hover:-translate-y-0.5">
                    Save Changes
                </button>
            </div>
        </form>
    </div>

</body>
</html>