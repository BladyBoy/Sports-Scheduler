<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create Sport | Sports Scheduler</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Inter', sans-serif; }
    </style>
</head>
<body class="bg-blue-50 h-screen flex items-center justify-center relative overflow-hidden">

    <div class="absolute top-0 left-0 w-full h-full bg-gradient-to-b from-blue-300/30 via-blue-200/20 to-transparent pointer-events-none -z-10"></div>

    <div class="bg-white w-full max-w-md p-8 rounded-2xl shadow-xl border border-blue-100 relative z-10">
        <div class="text-center mb-8">
            <h1 class="text-2xl font-bold text-slate-800">Create New Sport</h1>
            <p class="text-slate-500 text-sm mt-1">Add a new category for players to join.</p>
        </div>

        <form action="/sports" method="POST" class="space-y-6">
            <input type="hidden" name="_csrf" value="<%= csrfToken %>">
            
            <div>
                <label for="title" class="block text-sm font-bold text-slate-700 mb-2">Sport Name</label>
                <input type="text" name="title" id="title" required placeholder="e.g. Cricket, Basketball" 
                    class="w-full px-4 py-3 rounded-xl border border-slate-300 focus:ring-2 focus:ring-blue-500 focus:border-blue-500 outline-none transition shadow-sm placeholder-slate-400">
            </div>

            <div class="flex gap-4 pt-2">
                <a href="/sports" class="w-1/2 py-3 text-center text-slate-600 bg-slate-100 rounded-xl hover:bg-slate-200 transition font-medium">Cancel</a>
                <button type="submit" class="w-1/2 py-3 text-white bg-blue-600 rounded-xl hover:bg-blue-700 shadow-lg shadow-blue-500/30 transition transform hover:-translate-y-0.5 font-medium">Create Sport</button>
            </div>
        </form>
    </div>

</body>
</html>