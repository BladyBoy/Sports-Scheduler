<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> | Sports Scheduler</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Inter', sans-serif; }
    </style>
</head>
<body class="bg-blue-50 h-screen flex overflow-hidden">

    <aside class="w-64 bg-slate-900 text-white flex flex-col shadow-2xl z-20 border-r border-blue-900">
        <div class="p-6 text-center border-b border-slate-800 bg-slate-900">
            <h1 class="text-xl font-extrabold tracking-wider text-transparent bg-clip-text bg-gradient-to-r from-blue-400 to-emerald-400">
                SPORT SCHEDULER
            </h1>
            <div class="mt-4 flex items-center justify-center gap-2 p-2 bg-slate-800 rounded-lg border border-slate-700">
                <div class="w-8 h-8 rounded-full bg-gradient-to-br from-blue-500 to-emerald-500 flex items-center justify-center text-xs font-bold shadow-lg">
                    <%= user.firstName.charAt(0) %>
                </div>
                <div class="text-left">
                    <p class="text-xs text-slate-400">Welcome,</p>
                    <p class="text-sm font-semibold text-white"><%= user.firstName %></p>
                </div>
            </div>
        </div>

        <nav class="flex-1 p-4 space-y-2 overflow-y-auto">
            <a href="/sports" class="flex items-center gap-3 px-4 py-3 bg-blue-600 text-white rounded-xl shadow-lg shadow-blue-500/30 transition transform hover:scale-[1.02] ring-1 ring-blue-500">
                <i class="fa-solid fa-house"></i> 
                <span class="font-medium">Dashboard</span>
            </a>
            
            <% if (user.role === 'admin') { %>
                <p class="px-4 mt-6 mb-2 text-xs font-bold text-slate-500 uppercase tracking-wider">Admin Controls</p>
                <a href="/sports/create" class="flex items-center gap-3 px-4 py-3 text-slate-400 hover:text-white hover:bg-slate-800 rounded-xl transition group">
                    <i class="fa-solid fa-plus-circle group-hover:text-emerald-400 transition"></i> 
                    <span>Create Sport</span>
                </a>
                <a href="/reports" class="flex items-center gap-3 px-4 py-3 text-slate-400 hover:text-white hover:bg-slate-800 rounded-xl transition group">
                    <i class="fa-solid fa-chart-line group-hover:text-purple-400 transition"></i> 
                    <span>Reports</span>
                </a>
            <% } %>
        </nav>

        <div class="p-4 border-t border-slate-800 bg-slate-900">
            <a href="/signout" class="flex items-center justify-center gap-2 w-full px-4 py-2 text-red-400 bg-red-900/10 hover:bg-red-900/30 rounded-lg transition text-sm font-semibold">
                <i class="fa-solid fa-right-from-bracket"></i> Sign Out
            </a>
        </div>
    </aside>

    <main class="flex-1 overflow-y-auto relative bg-blue-50">
        
        <div class="absolute top-0 left-0 w-full h-96 bg-gradient-to-b from-blue-600/30 via-blue-400/20 to-transparent pointer-events-none -z-10"></div>

        <div class="p-8 max-w-7xl mx-auto">
            
            <header class="flex flex-col md:flex-row justify-between items-start md:items-center mb-8 gap-4">
                <div>
                    <h2 class="text-3xl font-extrabold text-slate-800 tracking-tight drop-shadow-sm">Dashboard</h2>
                    <p class="text-slate-500 text-sm mt-1 font-medium">Overview of all active sports and sessions</p>
                </div>
                
                <% if (user.role === 'admin') { %>
                    <span class="flex items-center gap-2 bg-white px-4 py-2 rounded-full shadow-sm border border-purple-100 ring-1 ring-purple-50">
                        <span class="w-2 h-2 rounded-full bg-purple-500 animate-pulse"></span>
                        <span class="text-purple-700 text-xs font-bold uppercase tracking-wide">Admin Mode</span>
                    </span>
                <% } %>
            </header>

            <% if (messages.success) { %>
                <div class="animate-fade-in-down bg-emerald-50 border-l-4 border-emerald-500 text-emerald-700 p-4 mb-6 rounded-r shadow-sm flex items-center gap-3" role="alert">
                    <i class="fa-solid fa-circle-check text-xl"></i>
                    <p class="font-medium"><%= messages.success %></p>
                </div>
            <% } %>
            <% if (messages.error) { %>
                <div class="animate-fade-in-down bg-red-50 border-l-4 border-red-500 text-red-700 p-4 mb-6 rounded-r shadow-sm flex items-center gap-3" role="alert">
                    <i class="fa-solid fa-circle-exclamation text-xl"></i>
                    <p class="font-medium"><%= messages.error %></p>
                </div>
            <% } %>

            <% if (user.role === 'admin') { %>
                <div class="bg-white/90 backdrop-blur-sm p-6 rounded-2xl shadow-sm border border-blue-100 mb-10">
                    <h3 class="text-xs font-bold text-slate-400 uppercase tracking-wider mb-4">Quick Actions</h3>
                    <div class="flex flex-wrap gap-4">
                        <a href="/sports/create" class="flex items-center gap-2 px-6 py-3 bg-blue-600 text-white rounded-xl hover:bg-blue-700 shadow-lg shadow-blue-500/20 transition transform hover:-translate-y-0.5 font-medium">
                            <i class="fa-solid fa-plus"></i> Create New Sport
                        </a>
                        <a href="/reports" class="flex items-center gap-2 px-6 py-3 bg-white border border-blue-200 text-slate-600 rounded-xl hover:bg-blue-50 hover:border-blue-300 transition font-medium">
                            <i class="fa-regular fa-file-lines"></i> View Reports
                        </a>
                    </div>
                </div>
            <% } %>

            <div class="flex items-center gap-3 mb-6">
                <h3 class="text-xl font-bold text-slate-700">Available Sports</h3>
                <div class="h-px bg-blue-200 flex-1"></div>
            </div>
            
            <% if (allSports.length > 0) { %>
                <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6">
                    <% allSports.forEach(sport => { %>
                        <a href="/sports/<%= sport.id %>" class="group relative bg-white rounded-2xl p-6 shadow-sm hover:shadow-xl border border-blue-100 hover:border-blue-400 transition-all duration-300 transform hover:-translate-y-1 overflow-hidden">
                            
                            <div class="absolute top-0 left-0 w-full h-1.5 bg-gradient-to-r from-blue-600 to-indigo-600"></div>
                            
                            <div class="flex justify-between items-start mb-4">
                                <div class="w-12 h-12 rounded-xl bg-blue-50 text-blue-600 flex items-center justify-center text-xl group-hover:bg-blue-600 group-hover:text-white transition-colors duration-300 shadow-inner">
                                    <span class="font-bold"><%= sport.title.charAt(0) %></span>
                                </div>
                                <span class="bg-blue-50 text-slate-500 text-[10px] font-bold px-2 py-1 rounded-full border border-blue-100 uppercase group-hover:bg-blue-100 group-hover:text-blue-700 transition">
                                    View
                                </span>
                            </div>

                            <h4 class="text-xl font-bold text-slate-800 mb-1 group-hover:text-blue-700 transition"><%= sport.title %></h4>
                            
                            <div class="flex items-center gap-2 text-sm text-slate-500 mt-4 pt-4 border-t border-slate-100">
                                <i class="fa-regular fa-user-circle"></i>
                                <span>Host: <span class="font-medium text-slate-700"><%= sport.User.firstName %></span></span>
                            </div>
                        </a>
                    <% }) %>
                </div>
            <% } else { %>
                <div class="text-center py-16 bg-white rounded-3xl border-2 border-dashed border-blue-200 shadow-sm">
                    <div class="w-20 h-20 bg-blue-50 rounded-full flex items-center justify-center mx-auto mb-4 text-blue-300">
                        <i class="fa-solid fa-trophy text-3xl"></i>
                    </div>
                    <h3 class="text-lg font-bold text-slate-700">No Sports Found</h3>
                    <p class="text-slate-500 mt-2 mb-6">There are currently no sports categories created.</p>
                    <% if (user.role === 'admin') { %>
                        <a href="/sports/create" class="inline-flex items-center gap-2 text-blue-600 hover:text-blue-700 font-semibold hover:underline">
                            <i class="fa-solid fa-plus"></i> Create the first one
                        </a>
                    <% } %>
                </div>
            <% } %>

        </div>
    </main>

</body>
</html>